{
  "name": "directus-backend",
  "version": "1.0.0",
  "description": "Directus backend for Railway deployment",
  "main": "index.js",
  "scripts": {
    "start": "directus start",
    "dev": "directus start",
    "build": "echo 'Build complete'",
    "postinstall": "directus bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["directus", "cms", "railway"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "directus": "^11.1.0"
  }
}
